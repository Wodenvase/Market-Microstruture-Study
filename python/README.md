# Python analysis

Use the `analyze.py` script to load `data/sim.csv`, compute tail index (Hill), a rough Hurst estimate, and create diagnostic plots.

Install requirements:

```bash
python -m pip install -r python/requirements.txt
```

Run:

```bash
python python/analyze.py data/sim.csv
```
